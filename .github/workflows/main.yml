name: CI-CD
'on':
  push:
     branches: ["main"]
  workflow_dispatch: null
env:
  ENV_WORKFLOW: Valor WORKFLOW
jobs:
  CD:
    steps:
    - name: print out example job
      uses: appleboy/ssh-action@v0.1.10
      with:
        host: 186.226.58.241
        username: root
        password: rjEAmIH4MO
        port: 22
        command_timeout: 30m
        script: | 
          echo "Nice Job!!!"
          docker clear  
          echo "Folder Deploy >>>> "
          cd ngweb-compose/
          echo "docker pull >>>> "
          docker pull jceleste/ngweb:latest     
          echo "docker-compose >>>> "          
          docker-compose up --build --detach 
